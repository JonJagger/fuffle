
$ curl install.meteor.com | sh
$ meteor create fuffle
$ cd fuffle
$ meteor uninstall --system
$ meteor add stylus
$ meteor add ejson
$ meteor add random
$ mrt add iron-router

$ git init
$ git remote rm origin
$ git remote add origin git@github.com:JonJagger/fuffle.git

$ git push -u origin master
$ meteor deploy fuffle.meteor.com

o) on answer page show N 'fake' answers in the same format
   as on the show page, but with the text blanked out.
o) for non-numeric data count frequency of each answer?
o) answer area size grows vertically dynamically
