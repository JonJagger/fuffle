
$ meteor create fuffle
$ cd fuffle
$ meteor uninstall --system
$ meteor add stylus
$ meteor add ejson
$ meteor add random
$ mrt add iron-router
$ git remote rm origin
$ git remote add origin git@github.com:JonJagger/fuffle.git

$ git push -u origin master
$ meteor deploy fuffle.meteor.com

I can't show the count in the answer template.
When someone else enters their answer, your page
refreshes and it delete the answer you are currently
typing.

o) CSS TLC
o) answers need their own overflow-y
o) if answers are numeric show n/mean/range/std-dev
