
$ meteor create fuffle
$ cd fuffle
$ meteor uninstall --system
$ meteor add stylus
$ meteor add ejson
$ meteor add random
$ mrt add iron-router
$ git remote rm origin
$ git remote add origin git@github.com:JonJagger/fuffle.git

$ git push -u origin master
$ meteor deploy fuffle.meteor.com

CSS TLC
answers need their own overflow-y
if answers are numeric show n/mean/range/std-dev
make feedback link to my Fellers-Walk rawgithub page
make unbiased a link to initial-base page (wikipedia?)